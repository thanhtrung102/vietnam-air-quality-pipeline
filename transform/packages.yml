packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]
  # Distributional / cross-column expectations the generic test layer can't express
  # (row-count > 0, monotone max >= avg >= min, physical non-negativity). Pulls
  # calogica/dbt_date transitively. Resolves dbt_utils to >=1.1.0 to satisfy both.
  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<0.11.0"]
